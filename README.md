# MilleGrilles web scraper

## ENV

```
CA_PATH=/var/opt/millegrilles/configuration/pki.millegrille.cert
CERT_PATH=/var/opt/millegrilles/secrets/pki.web_scraper.cert
KEY_PATH=/var/opt/millegrilles/secrets/pki.web_scraper.key
MQ_HOSTNAME=localhost
REDIS_HOSTNAME=localhost
REDIS_PASSWORD_PATH=/var/opt/millegrilles/secrets/passwd.redis.txt
FILEHOST_WEB_URL=https://localhost:3022
```
